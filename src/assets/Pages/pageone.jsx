import React from "react";

const Pageone = () => {
  return (
    <div>
      git fetch --all, git checkout -b local-branch-name
      origin/remote-branch-name
    </div>
  );
};

export default Pageone;
